# Generated by `wrangler types` from wrangler.jsonc -- not committed, see the comment in
# tsconfig.json: `pnpm typecheck`/`pnpm test` both automatically regenerate it before running, so
# these types are always in sync with wrangler.jsonc's bindings/vars, and it keeps this huge
# third-party-generated file (types for the entire Cloudflare platform API) out of the repo.
worker-configuration.d.ts

# Local `wrangler dev` environment variable overrides (e.g. overriding ALLOWED_ORIGIN back to
# http://localhost:5173) -- content varies per person and isn't version-controlled, see
# .dev.vars.example and the "Deploy" section of README.md.
.dev.vars
.dev.vars.*
!.dev.vars.example
