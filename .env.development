# Read by `pnpm dev` (Vite picks .env.development in development mode). Points at the local relay
# dev server (`pnpm relay:dev` runs wrangler dev on localhost:8787). Not a secret: this is a public
# connection address, so both .env.* files are committed.
VITE_PREVIEW_RELAY_URL=ws://localhost:8787
